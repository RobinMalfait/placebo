const user = {
  firstName: 'Angela',
  lastName: 'Davis',
  role: 'Professor',
}

console.log(user.name)

// ---

function compact(arr) {
  if (orr.length > 10) return arr.trim(0, 10)
  return arr
}

// ---

let a: { m: number[] }
let b = { m: [''] }
a = b
